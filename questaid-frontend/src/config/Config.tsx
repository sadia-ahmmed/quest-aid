const host = "localhost"
const port = "9090"
const aiPort = "5000"
const resourceUrl = `http://${host}:${port}`
const aiResourceUrl = `http://127.0.0.1:${aiPort}`


export { resourceUrl, aiResourceUrl }